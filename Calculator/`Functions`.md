`NormCdf()`：根据上下界求面积。
`NormPdf()`：根据x求高度。
`InvNorm()`：根据面积求z-score。比如，`InvNorm(0.2,0,1)`表示找到一个x，x左边的面积刚好等于0.2。

`BinomPdf(n, p, k)`：根据试验次数 `n`、成功概率 `p` 和具体成功次数 `k`，求恰好发生 `k` 次的概率（概率质量函数的高度）。  
    例如：`BinomPdf(10, 0.5, 3)` 表示抛10次硬币恰好出现3次正面的概率。 ^db0c5c

`BinomCdf(n, p, k)`：根据 `n`、`p` 和上界 `k`，求成功次数 ≤ k 的累积概率（即从0到k的概率之和）。  
    例如：`BinomCdf(10, 0.5, 3)` 表示抛10次硬币出现不超过3次正面的概率。

`InvBinom(n, p, area)`：根据 `n`、`p` 和累积概率 `area`，求最小的成功次数 `k`，使得 `BinomCdf(n, p, k) ≥ area`。  
    例如：`InvBinom(10, 0.5, 0.2)` 表示在二项分布中，找到最小的 `k`，使得累计概率至少为0.2（即左侧分位数）。

